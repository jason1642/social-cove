import * as React from 'react';

interface IGuestAccountButtonsProps {
}

const GuestAccountButtons: React.FunctionComponent<IGuestAccountButtonsProps> = (props) => {
  return (
    <View>
      
    </View>
  );
};

export default GuestAccountButtons;
