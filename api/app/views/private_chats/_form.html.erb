<%= form_with(model: private_chat) do |form| %>
  <% if private_chat.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(private_chat.errors.count, "error") %> prohibited this private_chat from being saved:</h2>

      <ul>
        <% private_chat.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
